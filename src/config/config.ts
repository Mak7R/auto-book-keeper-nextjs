import {endpoints} from "@/config/enpoints";
import {reactQueryKeys} from "@/config/react-query-keys";
import {localUrls} from "@/config/local-urls";

export const config = {
    endpoints,
    reactQueryKeys,
    localUrls
}